import { FilesUpload } from './files-upload';

describe('FilesUpload', () => {
  it('should create an instance', () => {
    const directive = new FilesUpload();
    expect(directive).toBeTruthy();
  });
});
